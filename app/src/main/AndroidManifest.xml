<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="orwir.starter">

    <application
        android:name="orwir.starter.StarterApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_rules"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity
            android:name=".ui.LoginActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.NewsActivity"
            android:windowSoftInputMode="stateAlwaysHidden"/>

        <service android:name="orwir.starter.service.FacadeKeeper"/>

    </application>

</manifest>
